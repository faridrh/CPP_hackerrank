#include <iostream>
#include <iomanip> 
using namespace std;

int main() {
	int T; cin >> T;
	cout << setiosflags(ios::uppercase);
	cout << setw(0xf) << internal;
	while(T--) {
		double A; cin >> A;
		double B; cin >> B;
		double C; cin >> C;

		int T;
        cin >> T;
        float n[3];
        // receive all elements of array
        for (int i=0; i<3; i++) {
        cin >>n[i];
        }
        int i;
        float n_new[3];
        switch (i){
            case 1:
                n_new[0]=n[0] ;
        }
    
        }
	}
